package com.goodbird.player2npc.companion.gui.settings;

/** Earliest lifecycle boundary at which a saved setting is guaranteed to take effect. */
public enum GuiSettingEffect {
    LIVE,
    NEXT_TASK,
    NEXT_LAUNCH
}
